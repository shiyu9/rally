# Changelog

## v0.6.3 (2026-09-25)

Opus 5.5 に合わせて、実装フェーズの終わり方と報告の形を足した改訂です。**原稿の書式と、生成される
`spec.html` は変わりません。** 既存のインタビュー記録を書き直す必要もありません。

### spec-interview

- **実装フェーズに「完了の定義」と「止まってよい条件」を書きました。** 完了は次の 4 つが揃ったときです:
  確定 Gherkin の E2E が全部 green/バッテリー適用結果表の 11 行が埋まる/`rally_test_audit.py` の
  (1)(表に書いた試験名の実在)が 0 件/煙試験の 1 行記録がある。完了までに止まってよいのは、
  観測できる振る舞いが未定義の仕様に出会ったとき(ミニインタビュー)と、破壊的な操作の前だけです。
  次の手を名指しするだけの途中要約、「続けましょうか」という申し出、作業を妨げない選択肢の列挙では
  止まりません。**壁打ちの段階で止まる規定(実装指示を待つ・まとめはユーザーが決める)は変わりません**
- **実装完了の報告を「あなた待ち/変えたこと/見つけたこと」の 3 見出しで、この順に書くようにしました。**
  スマホから開いて最初の数行しか読まない人でも、自分が次にすること(人間プローブの P1 と、何を見て
  何を答えるか)が先頭で分かります。頼むことが無いときも「あなた待ち: なし」と書きます
- **見た目の NG を、具体的なパターンの除外リストとして貯めるようにしました。** 人が触るフェーズで
  見た目の NG が出たら、`.claude/rally-checklist.md` の「見た目の除外リスト」節に「クリーム色の背景」
  のような具体的な形で追記し、実装フェーズで画面・出力の見た目を作る前に読みます。「ありきたりを
  避ける」のような形容では、別のありきたりに入れ替わるだけだからです。インタビューで見た目を聞く
  質問は増やしません
- **お題の文面から推測した前提を、確かめないまま仮決めや選択肢に入れない規定を足しました**
  (手順 1.5)。対象の数や置き場所、利用者が使う道具、成果物を開く環境の推測は軸の「未定」に置いて
  質問で確かめます。事実を聞く質問では「分からない」も独立した選択肢にし、確かめられない前提には
  どちらに転んでも困らない作りを候補に入れます。環境を聞いた答えは、見た目の好みではなく成果物が
  そこで動く条件として扱います
- **標準試験バッテリーに 2 項目を足しました**
  - カテゴリ 9「その環境を、宣言だけから作り直せるか」: 必須の外部依存は機械が読める形(依存の
    宣言ファイル等)で宣言し、その宣言だけから作った環境で試験が通ることを確かめる試験を 1 本置きます。
    文書に書いた導入手順は人しか読めず、新しい環境を作れません
  - カテゴリ 11「その項目を検査する試験が、まるごと skip し得ないか」: 理由の書かれた skip は静的検査を
    通りますが、ある環境で全部 skip するなら、その項目はそこで走る証拠がゼロです。理由があることと
    検出力があることは別です

## v0.6.2 (2026-09-23)

質問と試験の観点を足した改訂です。**原稿の書式と、生成される `spec.html` は変わりません。**
ただし標準試験バッテリーのカテゴリが 10 から 11 に増えるため、**実装フェーズで書く
「バッテリー適用結果」の表は 11 行**になります(過去のインタビュー記録を書き直す必要はありません)。

### spec-interview

- **標準試験バッテリーに「11. 試験そのものの検出力」を新設しました。** 既存の 10 カテゴリが
  「対象の何を試験するか」を並べているのに対し、この 1 つだけは**試験自身が対象に届いているか**を
  点検します。2 項目です
  - **下ごしらえが本体を通っていることを、1 件でよいので明示的に検査する** — 前提を満たさない
    下ごしらえ(本体が処理対象外と判定する入力)を与えた試験は、本体を一度も通らないまま pass します。
    緑であることからは、下ごしらえが正しいかは分かりません
  - **測る性質が、その実行経路に存在するか** — 人が読んで分かるか・対話でどう見えるかを測る試験は、
    出力を人が読まない経路(自動実行、ログだけが残る経路)で採ると判定そのものが成り立ちません
- **カテゴリ 9「環境依存」に「試験の実行条件と本番の差」を足しました。** 試験だけが使う短い
  一時パス・書き込み先を逃がす環境変数・偽の時計・縮めたサンプルは、本番で壊れる条件そのものを
  避けて走っていることがあります。OS の上限(パス長)・許可要求・OS が持つ時刻・実寸の件数に
  触れる対象は、本番と同じ置き場・同じ経路で動く試験を最低 1 本置いてください
  - 既存の「試験自体の環境非依存性」(子プロセスの入出力エンコーディングを env で固定する)と対になります。
    前者は「環境によって結果が揺れる」問題、今回のものは「どの環境でも緑だが本番の条件に一度も
    触れていない」問題で、向きが逆です
- **適用結果表の欄は 2 欄(試験名 / 該当なしの理由)のまま据え置きです。** カテゴリ 11 の 2 つ目の
  ように試験名を書けない性格の項目は、「なし」の欄に**どの経路で採ったか**を 1 行書いてください
  (例:「対話の窓で採取。自動実行では判定が成り立たないため該当なし」)
- **観点表(`references/checklist.md`)② 境界に、上限と取り込みの関係を聞く項目を足しました。**
  件数・サイズ・行数の上限を置く前に、その対象がインクルード・インポート・参照展開などで
  **自分で中身を増やす仕組み**を持つかを確認します。取り込みがあると、上限は取り込み先へ中身を
  移すだけで無効化できます

### spec-digest

- **`@spec` の機能名と `概要` は「他の欄は読まれない」前提で書く規則を足しました。** 第 7 章の
  機能一覧に出るのは `機能` と `概要` だけで、`処理` `実行契機` `入力` `出力` は載りません。
  機能名は説明なしで意味が通じる語にし、内部の処理名・変数名をそのまま使わないでください。
  `概要` は 1 行で全体が見えるように書きます
- **機能名を変えたら旧語で原稿全体を grep する**ことを明記しました。章をまたいで旧語が残ると、
  読者には別の機能に見えます

### Stop hook

- **締めの発言についての注意を、ヘッドレス(`claude -p`)の経路だけに限定しました。** 以前は
  「ヘッドレスやリモート」と書いていましたが、リモート接続では会話全体が相手に見えるため、
  最後の発言に要点を再掲する必要があるのはヘッドレスだけです

## v0.6.1 (2026-09-01)

文言と案内だけの修正です。**原稿の書式と、生成される `spec.html` は変わりません。**

v0.4.0 で第 6 章「データ仕様」が入って 6 章以降が 1 つずつ繰り下がり、同時に 3.2「外部との境界」が廃止されて処理の流れが 3.3 から 3.2 へ繰り上がっていました。この改訂に追随していない章・節番号が 31 箇所残っていたのを全て直しました。

### spec-digest

- **スキルが古い章番号を案内していたのを直しました。** 「第 6 章(機能一覧)と第 12 章(仕様の確定状況)は自動生成」と書いてありましたが、正しくは**第 7 章・第 13 章**です。同様に未確定事項は第 13 章ではなく**第 14 章**、機能仕様は第 7 章ではなく**第 8 章**です
  - 古い番号のまま原稿を書くと、自動生成の章を手で書いてしまい、ビルドが「章の番号・順序が違う」で止まります
- **ビルドが出すエラー文言 5 種が、存在しない「3.3」を案内していました。** 正しくは 3.2 です(エラーの「流れ」列が空・流れに現れないエラー・未定義の目的・流れの無い目的・目的が 0 件)。直せと言われた先が存在しない状態が解消されます
- **`format.md` の節名の例が、廃止済みの旧節名「起動と操作」のままでした。** この節名を原稿に書くとビルドが止まります。現行の「外部との境界」へ差し替えました

### spec-interview

- **仕様が書けるかの判定表(`references/spec-readiness.md`)を、現行の章立てに合わせました。** 番号だけでなく中身が v0.4.0 前のままで、手順 5 の整合チェックで**いま存在しない節を当てにいく**状態でした
  - 3.2「外部との境界」の判定表を第 5 章へ移して **5.1 外部との境界**(相手・向き・やり取りの中身)に
  - 旧 5.1〜5.3「起動と操作 / 結果の伝え方 / 生成物の構造」を、**5.2 経路ごとの仕様**の 7 項目(内容・手段・形式・契機・用途・エラー・検証)を問う表に作り替え
  - 3.3 適用場面 → **3.2 適用場面** へ繰り上げ。第 9 章の「流れが変わるか」の参照先も同様

## v0.6.0 (2026-08-30)

### Stop hook

- **README の「申し送り」節を更新するよう促すのをやめました。** ADR(`docs/adr.md`)への
  決定追記の促しは今までどおりです
  - 理由: 申し送りは rally の職分の外だからです。rally の枠内の「まだ決まっていないこと」には
    **保留**(spec-interview で次ラウンドへ持ち越す枠)と**第 14 章 未確定事項**
    (spec-digest が `U-nn` で出す表)という受け皿が既にあり、保留はインタビュー記録から
    仕様書へ自動で流れています。一方 README の申し送りに実際に書かれるのは「作業の残り」と
    「次のセッションが知るべき状態」で、これはタスク管理と引き継ぎの領分です
  - 促しには剪定の指示が無く追記だけが積まれるため、実プロジェクトで申し送りが README の
    7 割を占める・1 か月以上前に訂正済みの記述が残る、という状態が起きていました
- **すでに書かれている「申し送り」節は消しません。** 促さなくなるだけです。
  引き続き使う場合は各プロジェクトの運用として続けてください

### ドキュメント

- README の工程 1・2・5 の説明を v0.5.0 で入った機構に合わせました(確定を返すときの
  満たす例/満たさない例の対、整合チェックの章別判定と「誰が・いつ使うか」、実装後の静的検査と煙試験)

## v0.5.0 (2026-08-24)

### spec-interview

- rally試験015 の結果から: サンプル由来で 0 件だった軸はラウンド 1 の質問候補の先頭に置く
  (印だけでは選別で落ちる)。価値を明言された振る舞いはその場で E2E 化。人間判定の提示物は
  実物から機械的に導出し差異を併記。昇格した振る舞いは記録の Gherkin 全文にも追記。
  spec-digest に改名時の旧語 grep(原則 6)
- 標準試験バッテリー カテゴリ 9: 任意依存・環境で挙動が分岐する機能は両方の枝に試験を書く。
  バグの回帰試験は既定環境で red になり得る枝に置く(全組み合わせは干渉が確認された組だけ)
- spec-readiness の判定結果を記録に章別で残す(×・△は根拠必須、○は章名の列挙。
  「全章書ける」1 行のみは不可)
- 会話の途中で見落とし・誤りを自認したら、その場でインタビュー記録に 1 行メモを残す。観点表への
  転記はまとめか実装後の振り返りで(即転記はノイズ、メモ無しは忘却)
- 条件付きの事前承認(「問題なければそのまま進めて」)を実装開始の指示と見なさない。要約を見せて
  返答を待つ(rally試験014 で、この一言を受けて要約を貼った同じ発言内で実装に入っていた)
- rally試験014 の結果から: 否定例の規定を「確定を返すとき」に移設(選択肢形式の質問では
  言い直しが起きず、観点⑤の規定が適用されなかった)。軸の表の値に「サンプル由来・実物未確認」の
  印を付け、候補は実物を走査して埋める。ユーザーに確認作業を頼む前に、その環境でできるかを先に聞く
- **要件の「誤り」「不足」「過剰」「矛盾」を捕まえる層を追加。** 観点⑤の言い直しに
  **否定例**(要望を満たさない例)を添える規定(書いてあるが意図と違う要件は肯定例の言い直しでは
  表に出ない)。質問の前に**要望固有の軸の表**を書き出し、不足を「空欄」として見せる(手順 1.5)。
  確定した要件ごとに**「誰が・いつ使うか」**を書き、書けない要件は過剰の疑いとして確認する
  (整合チェック 6)。観点⑦**「対立軸」**(同期 vs 応答・性能 vs 一貫性・安全 vs 利便・
  コスト vs 冗長)を追加し、Stop hook の ADR 雛形に「対立した要件」を加えた
- **試験の実在と検出力の下限を機械で検査する `scripts/rally_test_audit.py` を同梱。**
  インタビュー記録のバッテリー表に書いたテスト名が `tests/` に実在するか、assert の無い test 関数、
  理由の無い skip、例外の握り潰しを検出する(Python は AST、JS/TS は簡易判定)。実装フェーズで
  バッテリー表の直後に実行し、結果と残した指摘の理由を記録に貼る。過去の試験回で「記録の名前と
  実在する関数名が 1 語違う」例があり、目視では捕まっていなかった
- **green の後に実装を 1 か所壊して red を見る煙試験**を実装フェーズに追加(試験が実装を見ているかの
  最小の証拠。壊す場所は人が選ぶので、その 1 か所についてだけの証拠である)
- **判定手段 A に「A-全域 / A-サンプル」の印**を付け、A-サンプル(列挙でしか書けていない試験)は
  バッテリー カテゴリ 7 で生成入力(property-based)か本体コードの assert に書き直せるかを必ず検討する

- **1 ラウンドの質問数の上限を 5 問から 4 問に変更。** `AskUserQuestion` が 1 回で出せる質問は 4 問までで、5 問は最初から出せなかった(過去の試験ログに残る 47 回の呼び出しは全て 4 問以下、5 問は 0 件)。上限を実際に出せる数に合わせ、`backfill-tests` の確認ラウンドと README の記述も揃えた

### spec-digest

- 同じ工程に `例外` を 2 つ書いたとき、黙って上書きせずビルドを止める(これまでは無関係な
  「E-nn が流れに繋がらない」エラーになっていた)。format.md に制約を明記
- 原則 4 に「ブラウザ上で初めて動く振る舞いは、属性や埋め込みの存在試験だけでは確定にしない」を追加

### Stop hook

- ADR・README の追記を促す文言に「追記後の締めの発言で直前の本文を置き換えない」を追加
  (ヘッドレス・リモートの経路では最後の発言だけが相手に届く)

## v0.4.1 (2026-08-11)

### spec-digest

- **関係図(4.2)で、辺が要素の箱を貫かなくなった。** 配置探索が見ていたのは線どうしの交差・ラベルの近接・ラベルと箱の重なりの 3 つで、線が第三の要素の箱を突き抜けることは評価に入っていなかった(5 要素の関係の張り方を総当たりすると 19 件で発生)
  - 貫通を交差より重い罰点として配置探索に入れ、それでも避けきれない組は箱をよけて 1 か所で折れる
  - ER 図(6)も同じ描画を使うため一緒に直る。IE 記法(鳥の足)の端も折れた向きに合う
  - `format.md` の `@graph` に振る舞いとして明記。原稿の書き方は変わらない
- **図の読みやすさを幾何で判定する試験**を追加。境界図(5)・処理の流れ(3.2)・関係図(4.2)・ER 図(6)の全てで、線が箱の中を走らないことを生成ページごとに確かめる

### spec-interview

- 試験バッテリーのカテゴリ 8(出力契約)に**「座標で組む出力(図・レイアウト)は、見た目を目視でなく幾何で判定する」**を追加。重なり・貫通・はみ出しを人の目に委ねると、入力の組み合わせ次第で崩れる経路が試験から丸ごと抜ける

## v0.4.0 (2026-08-11)

### spec-interview

- **判定手段の割り当て**を追加。確定・委任した項目ごとに **A 機械判定 / B 人間判定 / C 判定不能** を割り当てる
  - C を見つけたときは、要件の言い換えを求めるのではなく**「何を見て良し悪しを言うか」を聞く質問**に変え、A・B・スコープ外のどれかへ落とす
  - B はプローブ計画(何を見せ、何が見えたら OK か)を**実装前に**記録し、実装後にその計画を消化する
  - C が残ったまま実装に入る場合は、プランに列挙してユーザーの明示合意を取る
- **仕様が書けるかの判定**(`references/spec-readiness.md`)を、設計を確定する前の手順に追加
  - 仕様書の各章が確定内容だけで書けるかを当て、書けない章は設計の穴として扱う
  - 原因別に処理する: 観測できる振る舞いが未定義なら**質問ラウンドへ戻す**、外から見えない実装の都合は委任枠で仮決め、決めないと決めたならスコープ外として記録
- 観点表に**「集合を丸ごと処理する要望」**を追加。「フォルダ内の全部」「一覧をまとめて」のような要望では、その集合に**処理してはいけない要素**(下書き・非公開・テンプレート・アーカイブ済み・他人のもの)が混ざっていないかを聞く。「全部」は曖昧語である
- **プラン確定後に仕様が変わったときの更新先**を明記。Gherkin・ADR・判定手段の割り当て・spec-readiness の判定表の 4 か所を揃えて直す(一部だけ直すと記録同士が食い違う)

### spec-digest

- **第 5 章を「外部との境界」と「経路ごとの仕様」に作り替え**。境界図と本文が別の章にあり、図の矢印と記述が対応していなかった
  - `@io` のやり取り 1 本ごとに `@path` をぶら下げ、**内容・手段・形式・契機・用途・エラー・検証**の 7 項目を書く。5.2 は `@paths` とだけ書けば、機能仕様と同じブロック形式で並ぶ
  - **経路 ID(`I-01`…)は書かせない。** やり取りの並び順と向きから機械が振り、図の矢印ラベルにも前置される。相手と向きも行から入るため経路側には書かせない
  - 向きの数・順序の不一致、項目の欠落、`@paths` の書き忘れはビルドが止める
  - 経路の検証欄も E2E の集計に合流するため、境界の記述にも**実在しないテスト名の検出**が効く
  - 章構成: 3.2「外部との境界」を廃止し、第 3 章は 3.1 概要 / 3.2 適用場面 に繰り上げ
- **エラーと処理の流れの接続を検査**。9 章に書いたエラーが 3.2 のどの流れから離脱するかを示さないとビルドが止まる(流れが変わらない場合は「流れ不変」と明記する)。エラーだけが宙に浮く状態を無くした
- 機能一覧(第 7 章)の列順を **ID / 機能 / 概要 / 工程 / E2E / 判定** に変更
- **システム形状への依存を除去**。節名を「コマンドライン」ではなく「起動と操作」のように保ち、CLI・GUI アプリ・ライブラリのいずれでも同じ章立てで書けるようにした
- 検証欄が**日本語のテスト関数名**を受け付けるようになった(識別子として妥当なものは通す)。仕様に無い制限で生成できなくなる不具合を修正
- `format.md` を実装に同期。バリデーションの制約が仕様根拠か実装都合かを区別して書くようにした

## v0.3.0 (2026-08-08)

- `spec-digest` の出力を Markdown の読み物から **基本設計書としての単一 HTML(`docs/spec.html`)** に作り替え
  - 14 章構成: 文書情報 / システム概要 / システム構成 / インターフェース仕様 / データ仕様 / 機能一覧 / 機能仕様 / エラー仕様 / 非機能要件 / 適用範囲外 / 設計判断 / 仕様の確定状況 / 未確定事項 / 用語定義
  - 要素間の関係図と処理フローチャートを収録(CSS のみで作画。外部ライブラリも画像も持たない)
  - サイドバーの目次と全文検索(章・ID・テスト名)、昼夜 2 配色、単一ファイルで完結
  - 記述ごとに **確定 / 暫定 / 未固定** の判定が付く。暫定は特性化テストで固定しただけのもの、未固定は決定はあるが試験が無いもの
- **モデルは HTML を書かない。** 行形式の原稿 `docs/.spec-src.md` だけを書き、HTML は同梱スクリプト `skills/spec-digest/scripts/spec_build.py` が全て組み立てる
- **同じ数字を 2 か所に書く場所を無くした。** 機能一覧(6 章)・仕様の確定状況(12 章)・表紙の件数・工程番号・E2E 件数・判定は、機能仕様と処理フローから自動導出する
- 章立て・章名・必須の節・ID の連番・機能仕様の必須項目・検証欄の書式からの逸脱は、**行番号つき**でビルドが落とす。失敗時は既存の `docs/spec.html` を書き換えない
- 旧 `docs/spec.md` が残っている場合は、生成後にユーザーへ確認したうえで削除し、参照を張り替える
- 標準試験バッテリーの**無記録スキップを禁止**。従来は「対象に存在しない概念は飛ばす」とだけ書かれており、飛ばした判断が記録に残らなかったため、「該当なしと判断した」のか「そもそも見ていない」のかを後から区別できなかった
  - 実装後、10 カテゴリすべてについて「展開した試験名」または「該当なしと判断した理由」をインタビュー記録の適用結果表に残すことを必須化
  - 「あり」なのに試験名を書けないものは未実装として扱い、期待値が仕様から導けない場合はミニインタビューへ戻す
  - ADR に残した決定のうち外から観測できるものは対応する試験を持つ、という突き合わせを明記(決定だけあって試験が無い状態を、カテゴリ素通りのサインとして扱う)

## v0.2.0 (2026-07-27)

- 新スキル `backfill-tests`: rally を通さず作成・変更された実装に、後追いで不足している単体試験・E2E を補充する
  - 期待値の出所を三択で明示(仕様記録から導出/ユーザー確認済み/特性化テスト明示マーク)し、推測による現状追認の固定化を禁止
  - 中核の振る舞いはミニインタビューで確認、周辺は `characterization` マーク付きで暫定固定(後日昇格可能)
  - 確認済み Gherkin は `docs/interviews/` に合流、バグ確定挙動は「失敗を期待する」再現テスト(xfail strict 相当)として保存し、修正は promote-bug へ接続
  - 実データを持つプロジェクトでは観測・テスト実行を作業コピーへ隔離(実データ不変更を絶対のルールに明記)
- 標準試験バッテリー カテゴリ9(環境依存)に追記: 子プロセスを起動する試験は子の入出力エンコーディングを env で明示固定する(親側のデコード指定だけでは不十分)
- `promote-bug`: 「このバグはインタビューで防げたか」の判定を、完了報告だけでなくインタビュー記録・ADR にも一言残すよう明記

## v0.1.2 (2026-07-25)

- `spec-interview` の汎用観点表(⑤ 解釈の言い直し)に1項目追加: 外部ツールの制約を回避する選択肢を作る前に、その制約が仕様なのか設定で変えられるのかを一次情報で確かめる

## v0.1.1 (2026-07-18)

- README 改善: セキュリティチェック(OSV 照会・サプライチェーン検索・docs/packages/ 報告書)の独立した説明を追加、ワークフロー図の各フェーズの説明を追記

## v0.1.0 (2026-07-18)

初回公開。

- `spec-interview`: ラウンド制の壁打ちインタビュー(1 ラウンド最大 5 問・確定/委任/保留の三分類)、要件の整合チェック、技術選定(OSV 照会+調査報告書付きでユーザーと合意)、プラン承認後に実装
- `promote-bug`: バグ修正前の再現 E2E 昇格(失敗確認してから修正)。未定義仕様はミニインタビューへ
- `spec-digest`: E2E と ADR から固定フォーマットの仕様書 `docs/spec.md` を生成
- 標準試験バッテリー(境界値・異常系・データ頑健性など 10 カテゴリ)の自動展開
- Stop hook による ADR(`docs/adr.md`)・README 申し送りの追記促し(rally 使用プロジェクトのみで発火)
